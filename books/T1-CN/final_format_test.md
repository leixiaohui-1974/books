# 最终格式测试

## 一、简单公式测试

### 测试 A1：加粗+括号+简单公式（括号内有空格）
- **回水区蓄水面积**（ $A_s$ ）：说明文字

### 测试 A2：加粗+括号+简单公式（括号内无空格）
- **回水区蓄水面积**（$A_s$）：说明文字

### 测试 A3：加粗+冒号+简单公式
- **回水区蓄水面积**： $A_s$ ，说明文字

### 测试 A4：分两段写
- **回水区蓄水面积**：说明文字，符号为 $A_s$

---

## 二、复杂公式测试

### 测试 B1：复杂公式行内（当前格式）
**预测步**： $\hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})$

### 测试 B2：复杂公式改为独立多行块
**预测步**：

$$
\hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})
$$

### 测试 B3：复杂公式多行块（前面不换行）
**预测步**： $$\hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})$$

### 测试 B4：复杂公式改为列表+多行块
1. **预测步**：
   
   $$
   \hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})
   $$

---

## 三、EKF 完整格式测试

### 测试 C1：原格式（列表+行内复杂公式）
1. **预测步**： $\hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})$ ，协方差 $P_{k|k-1} = F_{k-1} P_{k-1|k-1} F_{k-1}^\top + Q$
2. **更新步**： $K_k = P_{k|k-1} H_k^\top (H_k P_{k|k-1} H_k^\top + R)^{-1}$

### 测试 C2：改为多行公式块
1. **预测步**：
   
   $$
   \hat{x}_{k|k-1} = f(\hat{x}_{k-1|k-1}, u_{k-1})
   $$
   
   协方差：
   
   $$
   P_{k|k-1} = F_{k-1} P_{k-1|k-1} F_{k-1}^\top + Q
   $$

2. **更新步**：
   
   $$
   K_k = P_{k|k-1} H_k^\top (H_k P_{k|k-1} H_k^\top + R)^{-1}
   $$
